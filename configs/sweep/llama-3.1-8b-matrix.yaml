# @package _global_
defaults:
  - override /inference: a100

sweep_name: llama-3.1-8b-matrix

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      model: llama-3.1-8b-base,llama-3.1-8b-instruct
      dataset: coding,completion,math,stories
